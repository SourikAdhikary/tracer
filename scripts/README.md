# Debug/test scripts from initial development
# These were used to debug mlx-vlm API, thinking block regex, and prompt tuning
# Main pipeline: src/tracer/pipeline.py (uses Auditor directly, no Scout)
